import colors from '../colors';
const BORDER_WIDTH = 0.5;

export default {
  textInputPaddingLeft: 20,
  inputHeight: 45,
  inputFontSize: 17,
  borderWidth: BORDER_WIDTH,
  borderStyle: {
    borderRadius: 2,
    borderWidth: BORDER_WIDTH,
    borderColor: colors.border,
  },
};
